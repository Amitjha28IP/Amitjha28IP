# Vaultwarden Local Development Configuration
# This file works for both Mac and Windows
# Copy this file to .env and update all values

# Database Configuration
DB_PASSWORD=vaultwarden_local_dev_password_2024

# Admin Token (generate with: openssl rand -base64 48)
# Mac: openssl rand -base64 48
# Windows: [System.Convert]::ToBase64String((1..48 | ForEach {Get-Random -Maximum 256}))
ADMIN_TOKEN=generate_your_admin_token_here_48_chars_base64

# Local Domain Configuration
DOMAIN=vault.local

# Google OAuth Configuration
# Get these from Google Cloud Console (see google-oauth-local-setup.md)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# SMTP Configuration (Optional for local development)
# Leave empty if you don't want email features during development
SMTP_HOST=
SMTP_FROM=
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_USERNAME=
SMTP_PASSWORD=

# Development Settings
# These are optimized for local development
LOG_LEVEL=info
EXTENDED_LOGGING=true
SIGNUPS_ALLOWED=true
INVITATIONS_ALLOWED=true
SHOW_PASSWORD_HINT=true

# File Upload Limits (for testing Send feature)
ATTACHMENT_LIMIT=104857600      # 100MB per file
USER_ATTACHMENT_LIMIT=1073741824  # 1GB total per user

# Send Configuration (Secure File Sharing)
SEND_PURGE_SCHEDULE=0 5 * * * *
SEND_DEFAULT_DELETE_DAYS=7
SEND_MAX_DELETE_DAYS=31
SEND_MAX_ACCESS_COUNT=100

# Optional: Duo Integration (if you have Duo Security account)
# DUO_INTEGRATION_KEY=
# DUO_SECRET_KEY=
# DUO_API_HOSTNAME=